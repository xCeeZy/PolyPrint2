<!--#region Страница оборудования-->
<Page x:Class="PolyPrint2.View.Pages.EquipmentPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Оборудование">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--#region Заголовок-->
        <TextBlock Grid.Row="0"
                   Text="Управление оборудованием"
                   Style="{StaticResource HeaderText}"/>
        <!--#endregion-->

        <!--#region Панель управления-->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="SearchBox"
                     Grid.Column="0"
                     Style="{StaticResource InputTextBox}"/>

            <Button x:Name="AddButton"
                    Grid.Column="2"
                    Content="Добавить"
                    Style="{StaticResource SuccessButton}"
                    Width="120"/>

            <Button x:Name="EditButton"
                    Grid.Column="4"
                    Content="Редактировать"
                    Style="{StaticResource PrimaryButton}"
                    Width="140"/>

            <Button x:Name="DeleteButton"
                    Grid.Column="6"
                    Content="Удалить"
                    Style="{StaticResource DangerButton}"
                    Width="120"/>
        </Grid>
        <!--#endregion-->

        <!--#region Таблица оборудования-->
        <DataGrid x:Name="EquipmentGrid"
                  Grid.Row="4"
                  Style="{StaticResource MainDataGrid}"
                  ColumnHeaderStyle="{StaticResource DataGridColumnHeader}"
                  CellStyle="{StaticResource DataGridCell}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ID_Equipment}" Width="60"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Модель" Binding="{Binding Model}" Width="200"/>
                <DataGridTextColumn Header="Серийный номер" Binding="{Binding Serial_Number}" Width="150"/>
                <DataGridTextColumn Header="Клиент" Binding="{Binding ClientName}" Width="200"/>
                <DataGridTextColumn Header="Состояние" Binding="{Binding Condition}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
        <!--#endregion-->
    </Grid>
</Page>
<!--#endregion-->
