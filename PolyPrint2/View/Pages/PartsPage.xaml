<!--#region Страница запчастей-->
<Page x:Class="PolyPrint2.View.Pages.PartsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Запчасти">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--#region Заголовок-->
        <TextBlock Grid.Row="0"
                   Text="Управление запчастями"
                   Style="{StaticResource HeaderText}"/>
        <!--#endregion-->

        <!--#region Панель управления-->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="SearchBox"
                     Grid.Column="0"
                     Style="{StaticResource InputTextBox}"/>

            <Button x:Name="AddButton"
                    Grid.Column="2"
                    Content="➕ Добавить"
                    Style="{StaticResource SuccessButton}"
                    Width="130"/>

            <Button x:Name="EditButton"
                    Grid.Column="4"
                    Content="✎ Редактировать"
                    Style="{StaticResource PrimaryButton}"
                    Width="160"/>

            <Button x:Name="DeleteButton"
                    Grid.Column="6"
                    Content="🗑 Удалить"
                    Style="{StaticResource DangerButton}"
                    Width="130"/>
        </Grid>
        <!--#endregion-->

        <!--#region Таблица запчастей-->
        <DataGrid x:Name="PartsGrid"
                  Grid.Row="4"
                  Style="{StaticResource MainDataGrid}"
                  ColumnHeaderStyle="{StaticResource DataGridColumnHeader}"
                  CellStyle="{StaticResource DataGridCell}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ID_Part}" Width="60"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Part_Name}" Width="*"/>
                <DataGridTextColumn Header="Артикул" Binding="{Binding Article_Number}" Width="150"/>
                <DataGridTextColumn Header="Цена" Binding="{Binding Price, StringFormat={}{0:N2} руб.}" Width="150"/>
                <DataGridTextColumn Header="Кол-во на складе" Binding="{Binding Stock_Quantity}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
        <!--#endregion-->
    </Grid>
</Page>
<!--#endregion-->
