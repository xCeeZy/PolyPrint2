<!--#region Окно добавления работы-->
<Window x:Class="PolyPrint2.View.Windows.AddWorkWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавить работу"
        WindowStartupLocation="CenterScreen"
        Width="700"
        Height="600"
        ResizeMode="NoResize"
        Background="{StaticResource WindowBrush}">

    <Grid Margin="30,30,30,12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!--#region Заголовок-->
        <TextBlock Grid.Row="0"
                   Text="Добавление выполненной работы"
                   Style="{StaticResource SubHeaderText}"/>
        <!--#endregion-->

        <!--#region Описание работы-->
        <TextBlock Grid.Row="2"
                   Text="Описание выполненной работы"
                   Style="{StaticResource LabelText}"/>
        <TextBox x:Name="DescriptionBox"
                 Grid.Row="4"
                 Height="80"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 VerticalContentAlignment="Top"
                 Padding="8"/>
        <!--#endregion-->

        <!--#region Стоимость-->
        <TextBlock Grid.Row="6"
                   Text="Стоимость работы (руб.)"
                   Style="{StaticResource LabelText}"/>
        <TextBox x:Name="CostBox"
                 Grid.Row="8"
                 Style="{StaticResource InputTextBox}"/>
        <!--#endregion-->

        <!--#region Использованные запчасти-->
        <TextBlock Grid.Row="10"
                   Text="Использованные запчасти"
                   Style="{StaticResource LabelText}"/>

        <Grid Grid.Row="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Grid.Column="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <ComboBox x:Name="PartsComboBox"
                          Grid.Column="0"
                          Height="32"
                          DisplayMemberPath="Name"/>

                <TextBox x:Name="PartQuantityBox"
                         Grid.Column="2"
                         Style="{StaticResource InputTextBox}"
                         Text="1"/>

                <Button x:Name="AddPartButton"
                        Grid.Column="4"
                        Content="Добавить"
                        Style="{StaticResource PrimaryButton}"
                        Width="100"
                        Height="32"/>
            </Grid>

            <DataGrid x:Name="UsedPartsGrid"
                      Grid.Row="2"
                      Grid.Column="0"
                      Style="{StaticResource MainDataGrid}"
                      ColumnHeaderStyle="{StaticResource DataGridColumnHeader}"
                      CellStyle="{StaticResource DataGridCell}"
                      Height="120">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Запчасть" Binding="{Binding PartName}" Width="*"/>
                    <DataGridTextColumn Header="Кол-во" Binding="{Binding Quantity}" Width="60"/>
                    <DataGridTextColumn Header="Цена" Binding="{Binding Price, StringFormat={}{0:N2}}" Width="80"/>
                    <DataGridTextColumn Header="Сумма" Binding="{Binding Total, StringFormat={}{0:N2}}" Width="80"/>
                </DataGrid.Columns>
            </DataGrid>

            <Button x:Name="RemovePartButton"
                    Grid.Row="2"
                    Grid.Column="2"
                    Content="Удалить"
                    Style="{StaticResource DangerButton}"
                    Width="80"
                    Height="32"
                    VerticalAlignment="Top"/>
        </Grid>
        <!--#endregion-->

        <!--#region Итого запчасти-->
        <TextBlock x:Name="PartsTotalText"
                   Grid.Row="14"
                   Text="Стоимость запчастей: 0,00 руб."
                   Style="{StaticResource NormalText}"
                   FontWeight="SemiBold"
                   HorizontalAlignment="Right"/>
        <!--#endregion-->

        <!--#region Кнопки-->
        <Grid Grid.Row="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="SaveButton"
                    Grid.Column="0"
                    Content="Сохранить"
                    Style="{StaticResource SuccessButton}"
                    Height="40"/>

            <Button x:Name="CancelButton"
                    Grid.Column="2"
                    Content="Отмена"
                    Style="{StaticResource SecondaryButton}"
                    Height="40"/>
        </Grid>
        <!--#endregion-->
    </Grid>
</Window>
<!--#endregion-->
