<!--#region Окно работ по заявке-->
<Window x:Class="PolyPrint2.View.Windows.WorksWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Работы по заявке"
        WindowStartupLocation="CenterScreen"
        Width="1000"
        Height="700"
        ResizeMode="NoResize"
        Background="{StaticResource WindowBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!--#region Заголовок-->
        <TextBlock Grid.Row="0"
                   Text="Работы по заявке"
                   Style="{StaticResource HeaderText}"/>
        <!--#endregion-->

        <!--#region Информация о заявке-->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="RequestInfoText"
                       Grid.Row="0"
                       Style="{StaticResource NormalText}"
                       FontWeight="SemiBold"/>

            <TextBlock x:Name="ProblemText"
                       Grid.Row="2"
                       Style="{StaticResource NormalText}"
                       TextWrapping="Wrap"/>
        </Grid>
        <!--#endregion-->

        <!--#region Список работ-->
        <Grid Grid.Row="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="12"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Button x:Name="AddWorkButton"
                    Grid.Row="0"
                    Content="Добавить работу"
                    Style="{StaticResource SuccessButton}"
                    Width="180"
                    HorizontalAlignment="Left"/>

            <DataGrid x:Name="WorksGrid"
                      Grid.Row="2"
                      Style="{StaticResource MainDataGrid}"
                      ColumnHeaderStyle="{StaticResource DataGridColumnHeader}"
                      CellStyle="{StaticResource DataGridCell}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Дата" Binding="{Binding Work_Date, StringFormat=dd.MM.yyyy}" Width="100"/>
                    <DataGridTextColumn Header="Описание работы" Binding="{Binding Description}" Width="*"/>
                    <DataGridTextColumn Header="Стоимость работы" Binding="{Binding Cost, StringFormat={}{0:N2} руб.}" Width="130"/>
                    <DataGridTextColumn Header="Запчасти" Binding="{Binding UsedPartsInfo}" Width="200"/>
                    <DataGridTextColumn Header="Стоимость запчастей" Binding="{Binding PartsCost, StringFormat={}{0:N2} руб.}" Width="140"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
        <!--#endregion-->

        <!--#region Итого-->
        <Grid Grid.Row="6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="4"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="4"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="WorksTotalText"
                       Grid.Row="0"
                       Grid.Column="1"
                       Text="Стоимость работ: 0,00 руб."
                       Style="{StaticResource NormalText}"/>

            <TextBlock x:Name="PartsTotalText"
                       Grid.Row="2"
                       Grid.Column="1"
                       Text="Стоимость запчастей: 0,00 руб."
                       Style="{StaticResource NormalText}"/>

            <TextBlock x:Name="GrandTotalText"
                       Grid.Row="4"
                       Grid.Column="1"
                       Text="Итого: 0,00 руб."
                       Style="{StaticResource NormalText}"
                       FontSize="16"
                       FontWeight="Bold"/>
        </Grid>
        <!--#endregion-->

        <!--#region Кнопка закрытия-->
        <Button x:Name="CloseButton"
                Grid.Row="8"
                Content="Закрыть"
                Style="{StaticResource PrimaryButton}"
                Width="120"
                Height="40"
                HorizontalAlignment="Right"/>
        <!--#endregion-->
    </Grid>
</Window>
<!--#endregion-->
